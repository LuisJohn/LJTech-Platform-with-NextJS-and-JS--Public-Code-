import React from 'react'

const Code = () => {
  return (
    <div className='ml-24 mt-12'>
        <div className="mockup-code">
            <pre data-prefix="$"><code>npm i -g ljtech-course-program</code></pre> 
            <pre data-prefix=">" className="text-warning"><code>installing...</code></pre> 
            <pre data-prefix=">" className="text-success"><code>Programming fundamentals</code></pre>
            <pre data-prefix=">" className="text-warning"><code>JavaScript to its nieces!</code></pre>
            <pre data-prefix=">" className="text-success"><code>Best of frontend with create-next-app@latest</code></pre>
            <pre data-prefix="#" className="text-warning"><code>Let's talk about Database with MongoDB</code></pre>
            <pre data-prefix=">" className="text-secondary"><code>Let's Build a project!</code></pre>
            <pre data-prefix=">>" className="text-success"><code>Let's deploy with best practices!</code></pre>
        </div>

    
    </div>
  )
}

export default Code